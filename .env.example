TELEGRAM_BOT_TOKEN=your-bot-token-from-botfather
PORT=3000

# Optionnel : active les suggestions IA (/optimize, node optimize.js) via l'API Claude.
# Sans cette cle, le score ATS et la comparaison a une offre restent fonctionnels.
ANTHROPIC_API_KEY=
